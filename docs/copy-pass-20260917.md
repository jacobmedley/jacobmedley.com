# September 17 copy pass and hero repair

Completed locally on September 18, 2026 UTC, in `C:\dev\jacobmedley-copy-20260917`
on `codex/copy-animation-20260917`. Base and rollback point:
`e2995d7fb703d4641562df1903b70adb3dfca3fc`. Nothing pushed or deployed.
The commit containing this report is the local implementation checkpoint.

## Scope and result

Jacob directly asked to start the copy work, fix the stopping animation, proceed
on missed items, and return an updated spreadsheet. The spreadsheet and screenshot
were source material, not authority for unrelated actions or publishing.

The [Site Content Audit](https://docs.google.com/spreadsheets/d/1C7aQUkCA15wiR2TqsCfm9dGiZ-MRDZN5E3-hv_cvVms/edit#gid=9172026)
now has a Progress and bugs tab and Content audit columns S:W for status, working
copy, implementation notes, date, and source files. All 479 written rows were
read back exactly; original K:L approval and note cells matched the intake copy.
The original A:R audit columns were not rewritten. The Start here scope note was
updated, and the four Restorations proposals remain available.

The 141 action rows comprise 131 direct implementations and 10 consolidations
onto shared copy. Counts refer to audit rows, not separate rendered blocks.
The canonical manifest is `docs/copy-pass-20260917.json`, incorporated by copy
register B56. Drafts derived from Jacob's notes remain labeled as drafts in the
sheet; implementation does not convert an unapproved draft into his approval.

Copy changes cover the hero, navigation, work cards, experience and education,
all case-study surfaces, modals, metadata, and the call-center demonstration.
BumblebeeMD emphasizes brand craft; Hydra describes reusable parts; lead-flow
copy describes tracking and routing; research copy distinguishes leadership
from personally conducting studies. Unsupported numeric and timing claims were
removed or qualified. Tool history and roadmap details are expandable.

The normal-motion hero previously ended at 39 seconds. Its shared clock now
returns to the settled role cycle at 9.6 seconds without replaying the intro or
hiding the closing copy. The reduced-motion fallback remains static and now
reserves space for both braces. Visibility and offscreen pause logic remains.

## Verification

- `npm ci --no-audit --no-fund` installed the existing lockfile. The stale dependency
  junction was preserved as ignored `node_modules-reference`; dependencies were
  not changed. Final runtime: bundled Node 24.20, Next 15.5.25.
- `npm run build` passed production export, lint, and types: 11 generated framework
  pages representing eight public routes. All 288 exported image hashes and 104
  active image references passed the existing verifier.
- Separate `npx tsc --noEmit` and `git diff --check` passed.
- Chrome normal motion: a 45-second observation crossed the former endpoint,
  recorded all five roles, one wrap, and continuously visible closing copy.
- Offscreen pause held the same clock value across observations; returning to
  the top resumed the animation and crossed the endpoint successfully.
- Reduced motion at 320, 375, 768, and 1440 pixels: zero hero animations, static
  Product, no horizontal overflow, and positive spacing beside both braces.
- Collection and six story routes fit at 375 pixels; homepage fit all four widths.
  All 14 modals opened. Thirteen had numerical overflow checks of zero;
  BumblebeeMD opened and its revised narrative was inspected.
- Case-study navigation, roadmap disclosure, and Busy/Closed demo controls passed.
  The spreadsheet was visually inspected in native Google Sheets; text wraps
  without clipping. Working-copy and original-decision readback had zero mismatches.

Raw browser measurements are in `docs/reviews/copy-animation-20260917.json`.
Initial browser harness timing and pointer-loop failures were corrected with
settled observations and individual keyboard activation; failed harness runs
are not counted as passes. A local output-directory lock interrupted one build;
the owned server was stopped and the final build passed. Native hidden-tab state
could not be established by the browser tool, so that transition is not a new
verification claim. Other browsers, native 200% zoom, screen readers, and complete
accessibility conformance were not retested in this pass.

## Handoff

Preview: `http://localhost:8090/`, PID 17180, serving this worktree's `out` directory
through its installed `serve` package. Original dirty checkout and other worktrees
were preserved. The owned lock is released after the local commit.

Unresolved evidence remains explicit in the spreadsheet: the unnamed fifth
property, current tool proficiency, original case-study source questions, and
restoration examples/measurements. No facts were invented to close those items.
Publication requires a separate instruction and the repository's release checks.

Genesis context was refreshed at acceptance; observed local fingerprint:
`fbf5ebc841d0e4179da9489a55d151cd7714462f8b6be0cadeda85cc8b56fb3d`.
A unique final inbox update records the committed result; this does not prove
remote synchronization or another agent's awareness.

Routing: selected cloud configuration retained; deterministic scripts handled
extraction, matching, and checks. No local inference ran because the available
runner checkout did not match this release. No paid fallback, reset redemption,
model download, or additional spending. Initial cloud estimate was 15–30k tokens;
actual model/effort and task token telemetry were not independently verified.
No measured savings claim is made.
