# Model-specific execution prompts

Use prompts 1–4 in order. G is optional and should not delay them. Choose the named
model in the app; the prompt alone does not switch it. Each task reads the shared
contract in the plan, so it does not need the entire conversation. Do not start a
later prompt before its preceding checkpoint is ready.

## G. Optional Genesis coordination: GPT-5.6 Luna / medium

```text
Prepare optional Genesis review capacity for the JacobMedley.com release. This
is a bounded coordinator eligibility check, not permission to edit website source
or infrastructure. Read X:/Genesis Exchange/START-HERE.md, Jacob's notes, current
brief, routing/coordination rules, local-inference and website workstreams, and
new/unresolved inbox notes. Read the Genesis contract in
C:/Users/jacob/.codex/worktrees/6fa6/jacobmedley.com/docs/release-plan-20260911.md.

Freshly verify the existing desktop-review runner, Qwen3.5 9B availability,
qualification, live lane ownership and exact source binding to the current owning
worktree/revision. Do not infer a free kernel lock from an old lockfile or empty
queue. The last profile pointed at C:/dev/jacobmedley.com; bounded excerpts alone
do not repair that mismatch. Honor the laptop inference hold. No lock removal,
profile change, model download, loaded-model displacement, paid fallback or reset.

If an already-authorized correct route exists, return its verified submission
contract to the website supervisor. Run only a specifically assigned bounded
read-only review, serially, with provenance and source-validated structured output;
at most two jobs plus one invalid-output retry total under the plan's caps. Never
edit source or execute model output. Otherwise record the exact blocker and skip
local inference so release work continues. No repairs are authorized by this prompt.
Append a unique Genesis handoff with actual checks and counts; do not edit shared
coordinator records unless this task is their designated authorized owner.
```

## 1. Finish implementation: GPT-5.6 Sol / medium

```text
Finish the remaining JacobMedley.com refinement locally in
C:/Users/jacob/.codex/worktrees/6fa6/jacobmedley.com. Read docs/STATUS.md first,
docs/working-agreement.md, docs/genesis-exchange.md, then
docs/release-plan-20260911.md and follow its shared execution contract. Verify
model availability and honor my selection. Expected branch is
codex/website-art-layout-20260910; d7999d5 is the application baseline, followed
by the release-plan documentation checkpoint. Reconcile actual HEAD/ownership.

Checkpoint A: finish original Stage 5 using the approved specification in
docs/stage1-checkpoint-20260909.md. Major homepage headings only: stacked on entry,
centered inline icon/title when sticky, frosted white, smooth transition, one
semantic heading, no pileup. Fix Education containment. On the standalone index
use one measured outcomes/stories context plus filter assembly. Preserve filtering,
anchors, focus, navigation, safe areas, modal layers and reduced motion. Test both
scroll directions, boundary crossings, filtering, keyboard and zoom. Save checkpoint A.

Continue in the same task to checkpoint B: finish every imagery/consistency item
in the release plan's remaining-work table. Replace the five DentalPlans supporting
cartoons and resolve Hydra, A/B, Personas and Personalization imagery using the
existing evidence and restrained shared visual language. Keep real screenshots,
canonical diagrams, source facts and originals. Prefer verified existing assets
and SVG geometry. Use imagegen only where necessary and within existing authorized
entitlement; no paid fallback. Register any copy/alt change before implementation.
Document each replacement or explicit source-based retention, with provenance.

Preserve completed horizontal cards, resume, motion and full-bleed modal behavior,
including the transparent native scrollbar and header. Sweep shared modal media,
buttons, icons, rules and rounded clipping for bounded inconsistencies. No new
redesign direction, factual claims, external resume-master edits or public links
to the standalone dashboard. Run affected development/export checks and make the
result reviewable. Use qualified Genesis review only if G cleared every condition;
otherwise continue with deterministic tools. Save checkpoint B, update STATUS last
and append a unique Genesis update. Stop before independent acceptance, push or PR.
```

## 2. Independent acceptance: GPT-6 Astra / medium

```text
Perform independent release acceptance of JacobMedley.com in
C:/Users/jacob/.codex/worktrees/6fa6/jacobmedley.com. Read STATUS first, the working
agreement, Genesis protocol, docs/release-plan-20260911.md and checkpoints A/B.
Follow the plan's shared contract; verify model availability and my selection.
Do not restart completed design stages or trust old pass labels for a new build.

Audit the complete unpublished update against every request group listed in the
plan, including original Stage 5/6 asks and the later cards, resume, imagery,
continuous-modal and conditional full-bleed instructions. Inspect the diff from
fresh main, preserved source data and all public/direct routes. Run the exact
applicable commands and coverage in the plan against development and final export.
Include visible native scrollbars, wheel/drag, open-modal resize, sticky/filter
boundaries, focus, touch, client navigation, font loading, pause/GIF freeze,
reduced motion, actual tab visibility, reflow, text spacing and 200% zoom/resize.
Measure real contrast and check corners/seams at fractional device scales.

Use installed browsers/devices; identify unavailable environments rather than
downloading them silently or claiming physical-device/WCAG certification. Fix
bounded defects, register copy changes first, and rerun only affected checks before
the final integrated matrix. Return substantive unresolved design choices to Sol's
checkpoint with a concrete finding. Do not waive release blockers to meet a budget.
Qualified local review may check a small evidence table; you own acceptance.

Write a compact acceptance matrix with namespaced annotation IDs, exact tested SHA,
commands, measurements, evidence paths and remaining limitations. Explicitly mark
release-ready or blocked and why. Save the local recovery checkpoint, STATUS last,
and a unique Genesis update. No push, PR, merge or deployment. Stop here.
```

## 3. Package and open the PR: GPT-5.6 Luna / medium

```text
Package the accepted JacobMedley.com update. Read STATUS first, the working
agreement, Genesis protocol, docs/release-plan-20260911.md and the final acceptance
checkpoint in C:/Users/jacob/.codex/worktrees/6fa6/jacobmedley.com. Follow their shared
contract. Verify current model availability and honor my selection.

This prompt authorizes pushing only the reviewed implementation branch and opening
its separate PR against main. It does not authorize merge or deployment. Recheck
GitHub main, existing PRs, exact branch HEAD, locks and Genesis first. PR #8 is
historical and merged; reuse a matching new open PR rather than creating duplicates.
Review the entire diff from main, including earlier unpublished dashboard work.
If source/base differs materially from accepted evidence, reconcile safely and
return affected acceptance to its owner before publication. No force-push/reset.

Reconcile current source-of-truth pointers and applied register states without
rewriting historical reports or making unsupported claims about external resume
masters. Write STATUS last and commit any documentation-only packaging changes.
Review that final diff. Push the named branch and create/update a concrete PR
describing final behavior, tested revision, validation, limitations and rollback.
Do not include unrelated changes, private data or large transient screenshot dumps.

Record final HEAD, acceptance SHA, current main, PR URL/state/checks, preview and
deployment state in the checkpoint and a unique Genesis update. No merge, main
push, deployment or automatic follow-on stage. Stop with the PR ready for review.
```

## 4. Merge, deploy and verify: GPT-6 Astra / high

Submit this prompt only when ready to publish the accepted PR. Its authorization
applies when Jacob submits it, not merely because it is stored in this document.

```text
Publish the reviewed JacobMedley.com update. I authorize merging its accepted PR
into main, the resulting existing SiteGround deployment, and verification of the
public release. I also authorize a reviewed revert of this release only if a
verified deployment regression requires recovery, preserving intervening work.

Read STATUS first, the working agreement, Genesis protocol,
C:/Users/jacob/.codex/worktrees/6fa6/jacobmedley.com/docs/release-plan-20260911.md,
and the acceptance/PR checkpoints. Follow the plan's shared execution contract.
Verify available model/effort and honor my selection. Resolve the exact PR from
the recorded URL and branch; do not guess or merge a different PR.

Freshly verify PR head/base, final diff, acceptance SHA, required checks,
mergeability, Genesis and ownership. Capture current production/main as rollback.
If material source/base changes invalidate acceptance, resolve them and rerun
affected checks before merge. Merge only the reviewed head via the normal PR
workflow; no direct or force push to main. Preserve dangerous-clean-slate:false,
unmanaged directories and the existing SiteGround workflow. No alternate hosting.

Observe the actual deployment run for the merged SHA to completion. Verify normal
public and cache-bypassed delivery of the homepage, standalone index, all six story
URLs, changed assets, resume, modal/full-bleed and sticky/filter flows. Inspect
X-Proxy-Cache, Last-Modified and visible release evidence; 200 alone is insufficient.
Compare /musings/ and /interaction-design-concepts/response-times/ before/after.
Purge stale SiteGround cache using existing authorized access and recheck. If access
is unavailable, report the exact blocker without claiming public verification.

For a verified regression, stop further changes and perform the scoped reviewed
revert/redeployment only if it preserves other work; verify public recovery and
cache again. Never reset main or erase intervening changes. Report any conflict.
Finish with exact PR/merge/deploy SHAs, workflow URL, public evidence, rollback,
remaining limitations and an accurate released/rolled-back/blocked state. Update
repository records and Genesis without an accidental second documentation-only
deployment. Stop after the release handoff; no unrelated follow-on work.
```
