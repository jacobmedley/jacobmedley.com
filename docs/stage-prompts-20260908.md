# New-chat stage prompts

Use the common opening below plus exactly one stage block. These prompts authorize
only that stage when Jacob sends them in a new chat. They do not authorize starting
all stages now. No task creation or automation is needed.

## Common opening — paste with every stage

```text
Continue JacobMedley.com in C:\Users\jacob\.codex\worktrees\8350\jacobmedley.com.
Perform only the stage below and stop at its checkpoint. Recommend the named model
and effort; verify availability and honor my actual selection without claiming an
unsupported switch. Use deterministic tools first. No paid fallback, model download,
usage reset, merge or deployment. Honor Genesis's laptop-inference hold; use local
models only through already authorized, qualified runner profiles with fresh route
and model checks. No local model edits or automatic execution of model output.

Read first, in order: docs/STATUS.md, docs/working-agreement.md,
docs/genesis-exchange.md, docs/copy-register.md, docs/jacob-style.md,
docs/voice-and-tone.md, docs/data-reporting.md; then X:\Genesis Exchange\START-HERE.md,
X:\Genesis Exchange\JACOB-NOW.md, X:\Genesis Exchange\CURRENT.md,
X:\Genesis Exchange\01-rules\COORDINATION.md,
X:\Genesis Exchange\01-rules\MODEL-ROUTING.md,
X:\Genesis Exchange\20-workstreams\website.md and relevant unresolved inbox notes.
Then read docs/website-refinement-plan-20260908.md and
docs/annotation-review-20260908.md, plus the preceding stage's checkpoint.

Check .tree-lock and inspect branch, HEAD and working tree before writing. Preserve
the known dashboard and planning changes; do not treat them as disposable. If the
tree differs from STATUS, reconcile ownership before overlapping writes. Use a
separate worktree for concurrent work. Do not reset or overwrite unrelated changes.
PR #8 merged at e6b3672; recheck current main and PR state rather than assuming the
old stacked-PR plan still applies. Keep the standalone page directly addressable,
separated from the homepage without authentication. Preserve canonical study copy,
metric framing, source links and diagrams. Author copy in the register first.

At completion, verify the stage, record its decisions and local checkpoint, update
STATUS last, reread Genesis and append a unique update event. Report actual branch,
commit, files, checks, preview state, deployment state, rollback and remaining work.
Don't push or open a PR unless this specific stage explicitly authorizes it.
```

## Stage 1 — Astra / high

```text
Execute Stage 1 only: baseline preservation and design decisions. Create a reviewed
local checkpoint of the existing dashboard and documentation before any branch/base
operation. Reconcile the merged-main baseline without losing this work. Inventory
icon ownership and thin-kit availability, inspect the new WebMD/BMD SVGs and Viva
photo sources, author B41 summary drafts and review B40/B42. Produce three finished,
reviewable horizontal card options outside production routes with desktop/narrow
variants. Record the motion, glass, type and sticky-header specifications. No
production UI edits. End with the options ready for my choice and all decisions in
the Stage 1 checkpoint. Do not choose an option on my behalf or start Stage 2.
```

## Stage 2 — Luna / medium

```text
Execute Stage 2 only from the approved Stage 1 specification: annotations 1, 6–8,
12, 14, 15 and 18. Apply registered B40 placement and approved B41 summaries;
correct scoped spacing, image-panel clipping, readable type and filter glass on
both surfaces. Preserve facts and existing interaction semantics. If Stage 1 copy
or tokens are not approved, finish independent specified changes and leave those
dependent items pending. Validate the documented responsive and contrast checks,
then record a local commit and stage checkpoint. No new motion or featured-art
redesign, remote push, PR, merge or deployment.
```

## Stage 3 — Sol / medium

```text
Execute Stage 3 only: shared desktop/mobile idle and fast-start/slow-end hover/focus
motion, connected Call Center nodes, rotating Personalization fan/warp, standalone
parity and the approved unique-icon registry. Include pause/resume, reduced motion,
offscreen suspension and stable touch navigation; register UI copy first. Test
actual changing animation, loop continuity, focus, client navigation and thin glyphs
on both surfaces. Increase effort only if concrete transform/behavior failures
justify it. Record the local commit and evidence. Do not start branded horizontal
cards or sticky headings, and do not publish.
```

## Stage 4 — Sol / medium

```text
Execute Stage 4 only using my recorded choice among the three horizontal options.
Import the selected verified assets from C:\dev\jacobmedley.com\public\assets\references\webmd
and C:\dev\jacobmedley.com\public\assets\references\bmd\SVG without changing originals.
Implement the chosen family for all five main featured cards: WebMD logo/health
field, DentalPlans tooth/depth schematic, BumblebeeMD bee/honeycomb and the approved
Hydra/One Park Financial treatments. Use the verified text-free Viva couple image.
Preserve modal actions, registered copy, diagrams and Stage 3 motion accessibility.
If the design choice or a source image is missing, don't invent it. Validate the
responsive artwork, glass contrast, focus and motion; checkpoint locally. No push,
PR, merge or deployment.
```

## Stage 5 — Sol / medium

```text
Execute Stage 5 only: sticky major section headers with centered inline icons,
frosted white backgrounds and smooth expanded-to-compact transitions. Follow the
approved hierarchy; fix Education's containing wrapper and coordinate standalone
section context with the existing filters. Preserve semantic headings, scrolling
anchors, focus visibility, modal layers and reduced motion. Test both directions
through every section and all changed boundaries, including mobile navigation and
zoom. Record a local checkpoint. Do not broaden this to every subheading or publish.
```

## Stage 6 — Astra / medium

```text
Execute Stage 6 only: independent acceptance of all 18 requests across homepage,
standalone dashboard and preserved story details. Run the exact checks in the plan
against development and production export; perform responsive, keyboard, touch,
font hydration, pause/reduced-motion, sticky/overflow and WCAG 2.2 AA checks. Record
actual contrast ratios and visual evidence, not an unsupported compliance claim.
Fix bounded failures; return unresolved design choices to their owning stage.
Document what was and was not tested, each annotation's status and the local recovery
commit. Update sources of truth and Genesis. No push, PR, merge or deployment.
```

## Stage 7 — Luna / medium

```text
Execute Stage 7 only after Stage 6 passes. I authorize pushing the reviewed
implementation branch and opening a separate PR; do not merge or deploy. Recheck
main/PR state and Genesis, preserve PR #8's history, and review the full final diff
and acceptance. Reconcile any changed base and rerun affected checks. Update docs
and STATUS last, commit, push only the case-study/refinement branch and open its
own PR against main. Record the actual final commit, PR URL/state, files, checks,
preview/deployment state and rollback in a unique Genesis update. No force-push,
push to main, deployment or automatic follow-on stage.
```
