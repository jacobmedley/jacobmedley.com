# September 12 mechanical visual-system continuation

Owner: Codex task 01a0978b. Jacob authorized continuation from `9508686`,
then explicitly approved branch `codex/website-visual-system-20260912` in the
isolated 4b5e worktree. Rollback baseline: `9508686`.

## Phases and checkpoints

1. Shared visual language: shadow-free nested cards, proportional icons,
   circular theme plaques, faded rules, contribution/technology taxonomy,
   Reveal section identities, Roadmap follow-up removal, matching modal fields.
   Check: source sweep, preserved copy/assets, representative modal layouts.
2. Resume: compact company/role spacing, real available brand assets, faded card
   edges, icon-left leadership cards, bottom-anchored native More/Less disclosures
   with reversible height animation. Check: copy equivalence, keyboard, focus,
   rapid reversal, reduced motion and enlarged text.
3. Mechanical hero and Hydra: responsive type tokens; staged brace/name/product
   mechanism; slower bounded role loop; editable component-theme schematic.
   Check: intro and settling, word fitting, hidden/offscreen suspension and
   reduced-motion static compositions. No visible motion control.
4. Acceptance: 320, 375, 768, 992, 1440 and 1875 widths, 200% text, semantic
   accessibility tree, modal focus return, overflow, types, lint and production
   export. Inspect screenshots; record actual failures and limits in design-qa.
   Write STATUS last, create recovery commit, append completion event, release lock.

Scope: shared source in app/globals.css, affected section/UI components,
lib/data/projects.ts, focused acceptance scripts and repository documentation.
No employment facts, dates, metrics or registered narrative changes. B53 records
the new labels before implementation. No Figma update is needed for this scope.
No push, PR, merge, deployment or replacement of reveal-clear-01.jpg is authorized.

## Coordination and cost

Exchange entry point, Jacob notes, current brief, coordination/model rules,
website card and relevant inbox read directly. The helper initially failed on the
historical Stage3 event missing base_fingerprint. Start event
`20260912T213943Z-e689187576dd43aaa41412626094964b` records this limitation.
Observed 2026-09-12T21:39:43Z; direct helper-algorithm inventory fingerprint:
`0e23907d70c28216c49a40ff90bb3db7a94238dca107c41ee45292e55fdc9164`.
Fresh direct reads and inventory comparison are required at checkpoints;
no remote synchronization or coordinator acknowledgement is asserted.

Selected model: GPT-6 Astra/high. Forecast: 35,000–65,000 cloud tokens across
implementation, review and retries; deterministic repository/browser work,
no local inference or paid provider. Exact telemetry is unavailable.
Avoided work must be labeled as an unmeasured estimate at closeout.

## Accepted implementation and durable rules

- Nested information surfaces are shadow-free; icon glyphs scale to 3rem with
  proportional padding and gaps. Project and section plaques remain circular
  and inherit their surrounding theme. The outer modal and highlighted Resume
  surface keep their existing depth.
- Contribution categories retain their faded divider and semantic group name.
  Technologies follow as distinct tinted badges. Visible group headings are
  removed. Measured technology text contrast exceeds 4.5:1 throughout the matrix.
- Icon thumbnails and their modal fields share project gradients. Fields move
  slowly and settle. Hydra uses editable SVG component cards whose palettes,
  controls and corner radii change together, plus Jacob's supplied Hydra mark.
- Resume uses real employer marks, compact company/role spacing, faded horizontal
  edges and four icon-left leadership cards. Native More/Less disclosures keep
  the control below their content; height transitions support rapid reversal.
- Hero type at 1875px is 54px name, 59.4px role and 26px closing line. A shared
  65-second Web Animations clock drives the brace mechanism and slower role
  changes, then settles on Human + Design. Resize preserves elapsed time and
  the settled state. Reduced motion exposes a complete static composition.
- Decorative motion has no visible pause controls. Offscreen and hidden-document
  suspension remain required. Preserve the single semantic name heading and
  stable assistive description; rotating decorative words are aria-hidden.
- B53 governs the exact Reveal labels, hero roles, More/Less labels and Roadmap
  image removal. Preserve every employment fact and registered narrative.

## Supplied assets

Copied byte-for-byte from `C:/dev/jacobmedley.com/public/assets/references/`:

| Source subfolder/file | Destination under public/assets/references |
|---|---|
| hec/hec-icon.svg | hec-icon.svg |
| moa/moa-icon.svg | moa-icon.svg |
| bgv/blue-green-dots.svg | blue-green-dots.svg |
| hydra/hydra-icon.svg | hydra-icon.svg |

OPF and DentalPlans use their existing featured SVGs. Asset hashes and source
equivalence are in `docs/reviews/visual-system-20260912.json`. Original image
files, including the removed Roadmap presentation source, remain unchanged.

## Acceptance and closeout

The final production export passed eight layout settings: six normal widths
and 200% text at 320/992px, with 14 modals per setting (112 cases). The final
intro also passed eight sampled phases at 320px/200% text. TypeScript, ESLint,
production build, source fidelity and whitespace checks passed. See the latest
entry in `design-qa.md` for repaired defects and verification limits.

The coordinator repaired historical Exchange metadata in a separate session,
preserving originals; the helper became operational again. Asset/direction event:
`20260912T215157Z-289cd526ba17414ea69c1c0c73c7d8dd`. Final acceptance freshly read
the entry point, current records and newer local-inference event at 22:07 UTC;
that event changes model inventory only and does not overlap website work.
Observed 2026-09-12T22:16:26Z fingerprint:
`fd668551ec6481d1b7c8f154dae327b9d07704dc39bdbfcb2e8067fd63c75ab2`, conflicts empty.
The final commit is sent in a new append-only Exchange update after committing.
Coordinator reconciliation and remote acknowledgement are not asserted.

Preview: production export on port 3013, node PID 45600, rooted in this worktree's
`out/`. The separate 6fa6 preview on 3012 was not stopped. No deployment occurred.
Rollback remains `9508686`. The deferred Reveal source replacement is still open.

Actual route: selected GPT-6 Astra/high, deterministic shell/browser checks,
no subagents or local inference. Exact cloud telemetry is unavailable. Closing
estimate: 45,000-65,000 cloud tokens including retries and review, zero local
inference tokens. Estimated avoided extraction/review work: 10,000-20,000 cloud
tokens, unmeasured rather than savings. No paid fallback or additional spending.
