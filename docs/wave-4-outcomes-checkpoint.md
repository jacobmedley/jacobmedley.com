# Wave 4 sourced outcomes checkpoint

Status: accepted locally on 2026-09-06. No deployment or push performed.

## Implementation

The case-study index now opens with a selected-outcomes dashboard: one prominent quantified platform result and five supporting qualitative records. Every record links to its full source story.

The dashboard does not own a second copy of any claim. `lib/data/case-studies.ts` stores only stable references—story slug plus proof and at-a-glance indexes—then resolves the displayed value, label, context, discipline tags, visual theme, and provenance from the canonical `docs/case-study-site-copy.json` data.

## Verification

- Six outcome records and six source-story links render at 375 and 1440px.
- The featured value is the approved `6 → 2` launch-time result.
- Every record carries no more than two discipline badges.
- No supporting card clips, and neither viewport has horizontal overflow.
- The supporting desktop grid balances three cards above two equal-width cards; the phone layout stacks all records.
- Focused evidence is generated by `scripts/parity/wave4-acceptance.mjs` under the ignored `scripts/parity/shots/wave4/` directory.

